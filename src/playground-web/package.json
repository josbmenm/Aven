{
  "name": "playground-web",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "scripts": {
    "start": "npx razzle start",
    "build": "npx razzle build",
    "test": "npx razzle test --env=jsdom",
    "start:prod": "NODE_ENV=production node build/server.js"
  },
  "dependencies": {
    "@aven-cloud/web": "^0.1.0",
    "express": "^4.16.3",
    "razzle": "^2.4.0",
    "react": "^16.3.1",
    "react-dom": "^16.3.1"
  }
}
